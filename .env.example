# Base URL of the yesen-crm-backend API (see that project's README for its
# own PORT/API_VERSION config). Defaults to the backend's local dev setup.
# VITE_API_BASE_URL=http://localhost:5001/api/v1
VITE_API_BASE_URL=https://yesen-crm-backend-5.onrender.com

